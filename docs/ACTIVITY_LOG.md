# Activity Log

## 2026-06-20

- Applied Modern Web Guidance to `index.html`: `prefers-reduced-motion` for scroll/animations, `content-visibility: auto` on below-fold sections, removed invalid `loading="lazy"` on `<video>`, and `scrollIntoView({ behavior: 'auto' })` so OS motion preference controls smooth scrolling.
- Installed Biome 2.5 (`@biomejs/biome`), added `biome.json` + `.editorconfig`, npm scripts (`lint:biome`, `format:check`, `check:biome`), and auto-formatted 30 source files (CRLF→LF). Format check passes; lint reports 25 errors (mostly a11y: missing `type="button"`, SVG titles).
- Fixed all 25 Biome lint errors: native `<dialog>` for map modal, `type="button"` on controls, `aria-hidden` on decorative SVGs, stable React keys, video captions track, JSON-LD/analytics suppressions. `npm run lint:biome` and `npm run format:check` now pass.
- Initialized Qlty (`.qlty/qlty.toml`) with osv-scanner, ripgrep, trufflehog. `qlty check -a --filter osv-scanner` reports 28 medium CVEs in package-lock (Next.js 15.5.15, tinymce/AdminJS, postcss, etc.). Biome runs via npm scripts, not qlty plugin (Windows JSON path bug).
- Full lint/format/security pass: fixed TS error in FAQ (`index + 1`), pointed `lint`/`check` scripts at Biome + `tsc`, updated Next to 15.5.19, added npm overrides for transitive CVEs, nested `next>postcss` override, excluded stale `contact/**` from qlty. `osv-scanner.toml` documents dev-only TinyMCE ignores. `npm run check`, `npm run build`, `npm audit`, and `qlty check -a` all pass (0 issues).
- Modernized site per MWG + frontend-design: Rubik/Nunito typography, CSS design tokens, skip link, semantic landmarks, native `<dialog>` mobile nav, FAQ via `<details>`, `content-visibility` on below-fold sections, video pause off-screen, `MotionConfig reducedMotion="user"`, `color-scheme`/`accent-color`, touch targets 44px.
- Content cleanup: cleared example posts from `data/posts.json` and RSS (0 items); removed seminar banner (`AnnouncementBadge`) and hero image; full-width header nav; added Telegram (@gurevichlena) and VK (elenagur30) contacts; new Yandex order form + VK Market links; order section updated.
- Configured global Cursor agent skill usage: `~/.cursor/cursor-instructions.md`, `~/.cursor/rules/always-use-skills.mdc`, `~/.cursor/skills/always-use-skills/SKILL.md`, project rule `.cursor/rules/always-use-skills.mdc`, and `chat.instructionsFilesLocations` for `~/.cursor` in user settings. Verified `chat.experimental.useSkillAdherencePrompt` is enabled.
- Vercel plugin `/status` workflow: confirmed GitHub→Vercel integration (last deploy success → cubichi.ru preview), local CLI unauthenticated, no `.vercel/` link. Ran `npm ci` + production build (18 static routes). Fixed `outputFileTracingRoot` in `next.config.ts` to silence incorrect workspace-root detection during builds.
